{
  "name": "patasycaos",
  "version": "1.0.0",
  "scripts": {
    "build:products": "node scripts/generar-productos.js",
    "build:products:test": "node scripts/generar-productos.js --test",
    "build:products:one": "node scripts/generar-productos.js --slug=",
    "build:pages": "node scripts/generar-paginas.js",
    "build:pages:one": "node scripts/generar-paginas.js --pagina=",
    "generate:all": "npm run build:products && npm run build:pages"
  }
}
